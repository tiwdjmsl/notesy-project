package com.notesy.service;

public class PaymentService {

    public boolean hasUserPurchased(String user, int noteId) {
        // Placeholder — prevents null errors
        return false;
    }
}
